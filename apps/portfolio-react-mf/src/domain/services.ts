/**
 * DOMAIN LAYER - Servicios de Dominio
 * Lógica de negocio que no pertenece a una entidad específica.
 *
 * Nota: LevelCalculator fue eliminado porque duplicaba Level.fromCount()
 * que ya existe en entities.ts. Usar Level.fromCount(clicks) directamente.
 */
